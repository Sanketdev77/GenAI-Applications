[project]
name = "ai-tutor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sanket", email = "sanketzz56@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ai-tutor = "ai_tutor:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
link-mode = "copy"
