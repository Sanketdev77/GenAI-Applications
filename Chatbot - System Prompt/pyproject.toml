[project]
name = "chatbot-system-prompt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sanket", email = "sanketzz56@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
chatbot-system-prompt = "chatbot_system_prompt:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
